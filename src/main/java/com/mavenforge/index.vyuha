<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>

<body>
    <h1>{{ greeting }}</h1>

    <ul>
        {if showItems == false}

        <li>No items available</li>


        {else}

        <li>Items are:</li>

        {/if}

        {for item in items}
        <li>{{ item }}</li>
        {/for}
    </ul>
</body>

</html>