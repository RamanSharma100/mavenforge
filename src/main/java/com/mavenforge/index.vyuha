<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>

<body>
    <h1>{{ greeting }}</h1>

    <ul>
        {if items != null}
        {for item in items}
        <li>{{ item }}</li>
        {/for}
        {else}
        <li>No items available</li>
        {/if}
    </ul>
</body>

</html>